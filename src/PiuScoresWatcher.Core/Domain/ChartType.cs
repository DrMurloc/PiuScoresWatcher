namespace PiuScoresWatcher.Core.Domain;

/// <summary>The three chart types RISE shows on a result screen; the PIU Scores API takes the same names.</summary>
public enum ChartType
{
    Single,
    HalfDouble,
    Double
}
